apiVersion: apps/v1
kind: Deployment
metadata:
  name: guestbook
spec:
  replicas: 1
  template:
    spec:
      containers:
        - name: guestbook
          image: gcr.io/google-samples/guestbook:v3
          env:
            - name: ENVIRONMENT
              value: "dev"

